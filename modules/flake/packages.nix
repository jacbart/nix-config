{
  perSystem =
    { pkgs, ... }:
    {
      # Packages are now defined in modules/pkgs.nix and modules/scripts.nix
      # This file is kept for compatibility but doesn't add new packages
    };
}
